name: Deploy

on:
  push:
    branches:
      - main  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: $cd01132e-3428-4310-9c89-6773100393d3
          heroku_app_name: "devopslab-agriciomoraes" #Must be unique in Heroku
          heroku_email: "agriciomoraes@terra.com.br"
